function knightMoves(start, end) {
	console.log("Working just fine");
	console.log(`Congrats Knight just got there in ${stepsCounter} steps`);
	console.log(`${path1}->${path2}->${path3}......`);
	return 0;
}